name: Build and push

on:
  push:
    branches:
      - dev
      - staging
      - prod
  workflow_dispatch:
    branches:
      - dev
      - staging
    paths-ignore:
      - 'charts/**'
  # Run workflow only if the code quality checks are fine for dev and staging
  workflow_run:
    workflows: [ "Code quality" ]
    types:
      - completed
    branches:
      - dev
      - staging
    paths-ignore:
      - 'charts/**'
  # Run workflow only if the code is release-tagged in prod
  release:
    # Types of releases to trigger this workflow
    types: [ published ]
    branches:
      - prod
      

jobs:

  dev-code-quality:
    if: github.ref == 'refs/heads/dev'
    uses: Julio-M/testing/.github/workflows/code-quality.yaml@$dev
  
  staging-code-quality:
    if: github.ref == 'refs/heads/staging'
    uses: Julio-M/testing/.github/workflows/code-quality.yaml@$staging

  job2:

    runs-on: ubuntu-latest
    # needs dev or staging depending on the branch
    needs: ${{ github.ref_name }}-code-quality
    steps:

      - name: Print branch name
        run: |
          echo "Branch name: ${{ github.ref_name }}


